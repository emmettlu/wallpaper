[package]
name = "wallpaper"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
reqwest = { version = "0.12", features = ["blocking"] }
serde_json = "1.0"
windows = { version = "0.61", features = ["Win32_UI_WindowsAndMessaging"] }

[profile.dev]
debug = false

[profile.release]
codegen-units = 1
debug = false
panic = "abort"
strip = true
lto = true
